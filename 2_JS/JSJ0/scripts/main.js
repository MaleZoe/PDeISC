// importamos la inicializacion del tema para el portal
import { inicializarTema } from '../Context/theme.js';

// iniciamos el script cuando carga la pagina
document.addEventListener('DOMContentLoaded', () => {
    inicializarTema();
});
