// Lógica simple para la barra de navegación (Bootstrap ya maneja el collapse en móvil,
// pero podemos agregar comportamientos extra si fuera necesario en el futuro).
console.log("Navbar script cargado.");
